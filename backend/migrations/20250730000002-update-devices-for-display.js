"use strict";

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // This migration was created but not implemented - adding empty structure
  },

  down: async (queryInterface, Sequelize) => {
    // No-op
  },
};
